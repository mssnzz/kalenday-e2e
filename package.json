{
  "name": "kalenday-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end and smoke tests for kalenday.com",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "codegen": "playwright codegen https://kalenday.com"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
